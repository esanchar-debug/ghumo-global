DATABASE_URL="postgres://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
SESSION_SECRET="replace-with-a-random-64-character-secret"
ADMIN_PASSWORD="ReplaceWithA15Char!Password"
APP_ORIGIN="https://book.ghumoglobal.com"

# Optional: required only for admin image uploads on Vercel.
BLOB_READ_WRITE_TOKEN=""

# Optional: required only if password-reset emails should be sent instead of logged.
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""
